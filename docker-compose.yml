services:
  server:
    image: ghcr.io/0ximjosh/tetris-server:latest
    build:
      context: .
      platforms:
        - linux/amd64
        - linux/arm64
    env_file: .env
    command: --host ${HOST:-0.0.0.0} --port ${PORT:-8080}
    restart: always
    ports:
      - 8080:8080
